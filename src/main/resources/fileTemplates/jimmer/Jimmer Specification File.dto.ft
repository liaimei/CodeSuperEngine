#if (${ENTITYPACKAGE} && ${ENTITYPACKAGE} != "")export ${PACKAGE_NAME}
    #if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")-> package ${PACKAGE_NAME}#end
#end

specification ${NAME}Specification {
    #allScalars
}